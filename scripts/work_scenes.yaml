alias: "Activate Work Scenes"
icon: mdi:briefcase

sequence:
  - action: scene.turn_on
    target:
      entity_id:
        - scene.dining_room_work

  - choose:
      - conditions:
          - condition: state
            entity_id: input_boolean.sam_home
            state: "on"
        sequence:
          - action: scene.turn_on
            target:
              entity_id: scene.study_work
            data:
              transition: 5.0
