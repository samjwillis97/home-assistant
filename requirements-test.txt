# Testing dependencies for Home Assistant automation testing
#
# IMPORTANT: It's recommended to install these in a Python virtual environment:
#   python3 -m venv venv
#   source venv/bin/activate
#   pip install -r requirements-test.txt

pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
freezegun>=1.4.0
pyyaml>=6.0

# Home Assistant core and testing framework
# Note: This will install Home Assistant and its dependencies
# Installation may take several minutes
pytest-homeassistant-custom-component>=0.13.0
